# Leave Management System — Pre-Cleanup Delete Plan

**No files were deleted, moved, or renamed in this pass. No database was modified. No old-stack
file (`src/`, `drizzle/`, `tests/`, old Docker/config files, `backup/*.sh`, `ci.yml`) was touched
or deleted.** This document is a plan, produced after Phase 1 (build replacements) and Phase 2
(verify them) were completed and validated. Actual deletion requires your separate, explicit
approval.

---

## What changed since PRE_CLEANUP_REVIEW.md

Five items were classified `REPLACE THEN DELETE` in the prior review. All five replacements now
exist and are validated:

| Item | Replacement built this pass | Validated how |
|---|---|---|
| `backup/*.sh` (old stack) | `backend-backup/backup.sh`, `restore.sh`, `test-restore.sh`, `crontab` + new `backup` service in `docker-compose.backend.prod.yml` | **Tested end-to-end against real dev data**: `pg_dump` against the live `backend-db` → restored into a throwaway container → verified real row counts via Django's actual table names (`accounts_employee`: 11, `departments_department`: 4, `leave_requests_leaverequest`: 20) → cleaned up. Media-archive half tested the same way (tar of real uploaded onboarding documents, listed, cleaned up). Real database never touched. |
| `.github/workflows/ci.yml` (old stack) | `.github/workflows/ci-app.yml` | YAML syntax validated (`python3 -c "import yaml..."` → valid, 4 jobs). Every individual command it runs (`npx tsc -b`, `npx vite build`, `npx vitest run`, `python manage.py check`, `python manage.py test`, `docker compose ... config`) has been independently proven working in this exact repo throughout this session. **Not yet run through actual GitHub Actions infrastructure** — that requires a push, which was not done. |
| `AGENTS.md` / `CLAUDE.md` (old-stack content) | `AGENTS.md` rewritten with current-stack architecture + the explicit rules requested (no Next.js, no Drizzle, no second stack, preserve data, prefer modifying over rebuilding, follow existing API architecture, run validation after changes). `CLAUDE.md` unchanged (`@AGENTS.md` include — already correct). | Content review (this is documentation, not executable). **Caveat**: this file's content is regenerated by `next dev` (per the file's own prior text) every time the old Next.js dev server runs — until `node_modules/next` etc. are removed, running `npm run dev` from the repo root will re-append Next.js boilerplate below the new content. Noted directly in the file itself. |
| `README.md` (old stack) | Rewritten — current-stack architecture, repo structure, real ports (5180/8012), real commands only, API structure, backup/restore, env config, dev workflow | Every command/port/URL in it was independently verified earlier in this session (health checks, `docker compose ps`, `tsc -b`, `vite build`, `vitest run`, `manage.py check`, `manage.py test` all directly run and confirmed). |
| `DEPLOYMENT.md` (old stack) | Rewritten as a short pointer to `DEPLOYMENT.backend.md` (already comprehensive — not duplicated, to avoid drift). Also updated `DEPLOYMENT.backend.md` §12 and its pre-launch checklist, which previously said backups were "not yet automated" — now stale given the new `backup` service, corrected to describe the actual mechanism. | Content review; cross-referenced against the actually-built `backend-backup/` scripts and `docker-compose.backend.prod.yml`'s new `backup` service definition. |

Given this, the classification for these 5 items changes from `REPLACE THEN DELETE` (blocked) to
`SAFE TO DELETE` (unblocked) in the table below — **their replacements exist, work, and are in
place**; only the actual deletion step remains, pending your approval.

## Phase 2 old-tech reference sweep — results

Searched the entire repository (excluding `node_modules`, `.git`, `.next`) for `next.js`,
`drizzle`, `leaflow`, and old service/compose-file names. Every occurrence found classifies
cleanly into one of two categories — **zero UNCERTAIN items found**:

1. **CURRENT / REQUIRED** — occurrences inside currently-active files (new docs, new backup
   scripts, `backend/*/settings.py`, `backend/*/services.py`, `frontend/src/lib/api-client.ts`,
   etc.) that mention the old stack **only** in explanatory/contrastive comments — e.g. "mirrors
   the Next.js app's GET /api/health," "not the old app's `leaflow` database." These are
   deliberate documentation of design rationale, not functional dependencies. No action needed.
2. **LEGACY AND SAFE TO DELETE** — occurrences inside old-stack files themselves (`src/`,
   `drizzle.config.ts`, `Dockerfile`, `.gitignore`'s Next.js rules, `package.json`,
   `docker-compose.yml`/`.prod.yml`, `.env*` for the old app, `tests/`, `scripts/bootstrap-admin.ts`,
   `.github/workflows/ci.yml`). Covered in the table below.

No occurrence required a `DOCUMENTATION REFERENCE THAT MUST BE UPDATED` classification — every
doc that needed updating (README, DEPLOYMENT, DEPLOYMENT.backend §12, AGENTS.md) was already
updated in Phase 1 above.

---

## Final Cleanup Candidate List

Evidence checklist per row (only listed where not uniformly "yes" — see notes column for
exceptions): (1) belongs to old stack, (2) not referenced by current React app, (3) not required
by Django, (4) not required by Docker, (5) not required by CI, (6) not required by backup/restore,
(7) not required by documentation, (8) no unique unmigrated functionality.

| Path | Old/New | Purpose | Evidence | Safe to Delete? | Replacement |
|---|---|---|---|---|---|
| `src/` (157 files) | Old | Next.js App Router — every page/API route of the original app | (1)✓ (2)✓ zero references from `backend/`/`frontend/`, either direction, re-confirmed this pass (3)✓ (4)✓ (5) referenced only by old `ci.yml` (6)✓ (7) referenced only by old README/DEPLOYMENT (now replaced) (8)✓ every feature confirmed migrated in `MIGRATION_AUDIT.md` §8 | **YES** | `backend/` + `frontend/` |
| `drizzle/`, `drizzle.config.ts` | Old | Next.js's ORM/migrations | Same as above; only referenced by `src/lib/db/*` and `package.json` | **YES** | Django models/migrations |
| `public/` | Old | Next.js static assets | Only referenced by `src/app/layout.tsx` etc. **Exception**: contains `agrileaf-logo.png`, not currently used by `frontend/` at all | **MANUAL REVIEW** | Copy the logo into `frontend/` first if it should carry forward — your call, not inferred |
| `tests/`, `playwright.config.ts`, `vitest.config.mts` | Old | Next.js's own Vitest/Playwright suites, exercise `src/app/api/*` directly | Only referenced by old `ci.yml` and root `package.json` scripts | **YES** | `backend/*/tests/`, `frontend/src/**/*.test.tsx` |
| `scripts/bootstrap-admin.ts` | Old | Next.js-specific first-admin script | Only referenced by root `package.json`'s `bootstrap-admin` script and old README (now replaced) | **YES** | `backend/accounts/management/commands/bootstrap_admin.py` |
| `vendor/xlsx-0.20.3.tgz` (root) | Old | Next.js's vendored xlsx copy | Only referenced by root `package.json` | **YES** | `frontend/vendor/xlsx-0.20.3.tgz` (separate, already in place) |
| `Dockerfile`, `docker-compose.yml`, `docker-compose.prod.yml`, `Caddyfile` (root) | Old | Next.js Docker stack | Only reference each other / manual invocation | **YES** | `backend/Dockerfile`, `frontend/Dockerfile`, `docker-compose.backend*.yml`, `Caddyfile.backend` |
| `next.config.ts`, `next-env.d.ts`, `components.json`, `eslint.config.mjs`, `postcss.config.mjs`, `sentry.server.config.ts`, `src/instrumentation.ts` | Old | Next.js/shadcn/Sentry-for-Next.js build config | Next.js build only | **YES** | N/A |
| `tsconfig.json`, `tsconfig.tsbuildinfo` (root) | Old | Next.js TS config + build cache | Next.js build only | **YES** | `frontend/tsconfig.json` |
| `package.json`, `package-lock.json` (root) | Old | Next.js dependency tree | `npm` commands, old `ci.yml` | **YES** | `frontend/package.json`, `backend/requirements.txt` |
| `.env`, `.env.example`, `.env.production.example` (root) | Old | Next.js env templates | Next.js runtime only | **YES** | `.env.backend*` |
| `node_modules/` (root, 910MB), `.next/` (225MB) | Old | Generated/gitignored build artifacts | N/A — not source | **YES** | N/A |
| `.github/workflows/ci.yml` | Old | CI for the Next.js app only | Replacement (`ci-app.yml`) built and validated this pass — see table above | **YES** | `.github/workflows/ci-app.yml` |
| `backup/backup.sh`, `restore.sh`, `crontab`, `test-restore.sh` | Old | Backup tooling hardcoded to `leaflow`/`db`/`docker-compose.prod.yml`/`app` | Replacement (`backend-backup/`) built and tested end-to-end this pass — see table above | **YES** | `backend-backup/` |
| `.DS_Store` (root) | Neither | macOS Finder artifact | Not part of any app | **YES** | N/A |
| `backend/`, `frontend/` | **New** | The current application | Actively running, actively serving traffic | **NO — KEEP** | — |
| `docker-compose.backend.yml`, `docker-compose.backend.prod.yml` | **New** | Current stack's Docker Compose | Runs the current application (verified this session) | **NO — KEEP** | — |
| `Caddyfile.backend`, `frontend/nginx.conf`, `frontend/nginx-proxy-params.conf` | **New** | Current stack's production reverse proxy | Required by `docker-compose.backend.prod.yml` | **NO — KEEP** | — |
| `.env.backend`, `.env.backend.example`, `.env.backend.uat.example`, `.env.backend.production.example` | **New** | Current stack's env config, including the real local `.env.backend` | Required to run the current stack | **NO — KEEP** | — |
| `DEPLOYMENT.backend.md` | **New** | Current stack's deployment guide | Now the canonical deployment doc (README.md and DEPLOYMENT.md both point to it) | **NO — KEEP** | — |
| `backend-backup/` | **New** | Current stack's backup/restore tooling | Referenced by `docker-compose.backend.prod.yml`'s `backup` service and `DEPLOYMENT.backend.md` §12 | **NO — KEEP** | — |
| `.github/workflows/ci-app.yml` | **New** | Current stack's CI | Not yet run on GitHub Actions (would need a push) | **NO — KEEP** | — |
| `README.md`, `DEPLOYMENT.md`, `AGENTS.md`, `CLAUDE.md` | **New** | Current-stack documentation | Root-level docs, repo-wide agent instructions | **NO — KEEP** | — |
| `.dockerignore` | Shared | Applies to whichever Dockerfile is built | Still needed once old Dockerfiles are gone — review its contents at that point, don't delete | **NO — KEEP** | — |
| `MIGRATION_AUDIT.md`, `PRE_CLEANUP_REVIEW.md`, `PRE_CLEANUP_DELETE_PLAN.md` (this file) | **New** | This project's own audit trail | You may want to keep these as historical record even after cleanup, or delete them once cleanup is done — your call | **YOUR CALL** | — |

---

## Report

**Files ready for deletion** (25 rows above marked `YES`, ~180 files + 2 large generated
directories): the entire old-stack tree — `src/`, `drizzle/`, `tests/`, old Docker/config/env
files, `scripts/bootstrap-admin.ts`, `vendor/xlsx-0.20.3.tgz` (root), `backup/*`, old `ci.yml`,
`.DS_Store`, root `node_modules/`/`.next/`.

**Files requiring replacement**: none remaining — all 5 identified in the prior review now have a
built, validated replacement (see table at the top).

**Files already replaced**: `backup/*.sh` → `backend-backup/`; `.github/workflows/ci.yml` →
`.github/workflows/ci-app.yml`; `AGENTS.md`/`README.md`/`DEPLOYMENT.md` → rewritten in place
(old content preserved only in git history, since these are single canonical files, not
old-and-new pairs).

**Files that must remain**: `backend/`, `frontend/`, both `docker-compose.backend*.yml`,
`Caddyfile.backend`, nginx config, `.env.backend*`, `DEPLOYMENT.backend.md`, `backend-backup/`,
`.github/workflows/ci-app.yml`, the 4 root docs, `.dockerignore`.

**Uncertain files**: `public/agrileaf-logo.png` — the only item in this entire audit still marked
`MANUAL REVIEW`. Everything else resolved to a clear `YES`/`NO — KEEP` this pass.

**Remaining old-stack references**: only inside the old-stack files themselves (which are the
subject of the deletion plan) and deliberate contrastive comments inside current/required files
(Phase 2 sweep — zero accidental or functional old-tech dependency found anywhere).

**Backup status**: 🟢 built, tested end-to-end against real data, documented. No longer a blocker.

**CI status**: 🟢 replacement built and syntax-validated; individual commands proven; not yet
exercised on actual GitHub Actions (would require a push — out of scope for this audit-only pass).

**Documentation status**: 🟢 `README.md`, `DEPLOYMENT.md`, `DEPLOYMENT.backend.md`, `AGENTS.md` all
now describe only the current stack.

---

## READY FOR CLEANUP

Every condition from the prior review's readiness bar is now met:

| Condition | Status |
|---|---|
| Migration complete | ✅ |
| Current application works | ✅ |
| Frontend build/TypeScript/tests pass | ✅ |
| Lint passes where configured | N/A (not configured) |
| Backend tests pass | ✅ (deterministic since the weekend-guard fix) |
| Docker works | ✅ |
| No required legacy code remains | ✅ |
| **Documentation/manual-review items resolved** | ✅ **now met** — backup, CI, AGENTS.md/CLAUDE.md, README/DEPLOYMENT all replaced and validated |
| No database/data changes required | ✅ — none were made |

The only open item is `public/agrileaf-logo.png` (one file, `MANUAL REVIEW`) — it does not block
cleanup of everything else; decide on it whenever convenient, before or after deleting `public/`'s
other contents.

**Stopping here. No deletion has been performed. Waiting for your explicit approval before
carrying out the cleanup described in the table above.**
